# Tests

This directory contains the test suite for the project.

The main tests are in `src/validation/integrity_tests.py`.

To run tests:

```bash
pytest src/validation/integrity_tests.py -v
```

Or use the convenience script:

```bash
bash src/validation/ci_run.sh
```
